@model List<NewsPortal.Models.Post>

<h2>Noticias</h2>
<ul>
    @foreach (var post in Model)
    {
        <li>
            <a asp-controller="News" asp-action="Details" asp-route-id="@post.id">@post.title</a>
        </li>
    }
</ul>
